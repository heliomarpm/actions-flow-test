name: 2. Pull Requests Tests
run-name: PR ${{ github.event.workflow_run.head_branch || github.head_ref || github.ref_name }} → ${{ github.base_ref }}

on:
  workflow_run:
    workflows: ["1. Quality Assurance"]
    types:
      - completed
  # push:
  #   branches:
  #     - feature/**
  #     - develop

permissions:
  contents: write
  pull-requests: write

jobs:
  app:  
    if: >
      ${{
        github.event.workflow_run.conclusion == 'success'
        && github.event.workflow_run.head_branch != 'main'
      }}

    uses: heliomarpm/actions-flow/.github/workflows/cd-pull-request.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    with
      pr_body: >
        🚀 Pull Request Automatizado
        ---
        {{QUALITY_GATE_RESULT}}
